$("#edit_dialog").html("<%= escape_javascript(render(:partial => 'pacients/edit_form', :locals => {:user => @user})) %>");
$("#edit_dialog").dialog({ position: ['left','top'], resizable: false, minHeight: 30 });

